# 이 저장소에서 작업할 때

## 작업 완료 보고 규칙

작업이 끝나면(배포까지 마친 뒤) 사용자에게 실제 접속 URL을 알려줍니다.
예: `https://webyigit.github.io/wweebbyyii/magok-matjip/`

- 링크는 클릭 시 **외부 브라우저**로 열리게, 일반 URL 텍스트로 줍니다.
- 아티팩트나 인앱 미리보기로 대체하지 않습니다 — 사용자가 실제 배포된 페이지를
  자기 브라우저로 열어서 확인해야 합니다.

## 끝내기 전에 반드시 자체 검증 5회

`docs/magok-matjip/index.html` 을 건드렸다면, **작업을 마쳤다고 보고하기 전에** 반드시:

```bash
node verify.mjs            # 23항목 × 화면 4종 × 5회 = 460번
```

- **실패가 하나라도 있으면 완료가 아닙니다.** 고치고 다시 돌리세요.
- 사용자에게는 통과/실패 수와 실패 항목을 요약해 보고하세요.
- **검증이 끝나기 전에 "고쳤다"고 보고하지 마세요.** 돌고 있으면 돌고 있다고 하세요.
- **고쳤으면 아티팩트를 다시 배포하세요.** 저장소에만 커밋하면 사용자가 보는 화면은
  그대로입니다. 실제로 이것 때문에 "아직 안 된다"는 말을 들었습니다.
- 간헐적으로만 터지는 버그를 잡으려고 5회 반복합니다. 5회 중 1회만 실패해도
  실패로 봅니다.
- 돌리기 전에 **예전 검증 프로세스가 남아 있지 않은지 확인하세요.** 두 번이 겹쳐 돌면
  로그가 섞여서 이미 고친 버그가 아직 남아 있는 것처럼 보입니다.
  `pkill -f verify.mjs` 는 자기 자신이 실행 중인 셸까지 죽이므로 쓰지 마세요.

playwright 가 필요합니다. 없으면 `npm i playwright` 로 설치하세요.
크롬 경로가 다르면 `CHROME_PATH` 환경변수로 지정합니다.

## 검증에 항목을 추가해야 할 때

**버그를 고쳤으면 그 버그를 재현하는 항목을 `verify.mjs` 에 추가하세요.** 지금 들어 있는
10~13번은 전부 실제로 냈던 버그입니다:

- 칩 줄을 마우스로 끌 수도 휠로 넘길 수도 없었음
- 끌다가 손을 떼면 그 자리 분류가 눌려 버림
- 누르자마자 `.dragging` 이 붙어 `pointer-events: none` 이 걸리는 바람에
  분류 선택이 통째로 죽음
- 남은 거리보다 휠 한 번이 크면 휠이 아예 안 먹음

같은 걸 두 번 내지 않으려면 항목으로 남겨야 합니다.

## 데이터에 대한 원칙

**별점·가격·메뉴·사진을 지어내지 마세요.** 비어 있으면 비워 두고, 화면에는 왜 비었는지
안내가 나가게 합니다. 실제 가게 정보라 틀린 값을 넣으면 그 가게에 피해가 갑니다.

데이터는 `index.html` 안 `<script type="application/json" id="places-data">` 블록에
있고, `fetch_places.py` / `crawl_naver_place.py` 가 이 블록을 다시 씁니다. 직접 써 둔
소개글(`note`)은 다시 받아와도 덮어쓰지 않습니다.

## 파일 구성

| 파일 | 하는 일 |
|---|---|
| `docs/magok-matjip/index.html` | 페이지 전체 (데이터·스타일·스크립트 한 파일) |
| `verify.mjs` | 자체 검증 |
| `fetch_places.py` | 네이버 지역검색 API 로 가게 목록·좌표 |
| `crawl_naver_place.py` | 네이버 플레이스에서 별점·인기메뉴·좌표 (로컬 크롬) |
| `update.py` / `update.bat` | 위 과정을 한 번에 (윈도우용) |
| `auto_post.py` | 네이버 블로그 자동발행 (별개 프로젝트) |

사용자는 윈도우에서 작업합니다. 명령을 안내할 때 PowerShell 5.1 은 `&&` 를 못 쓰고,
여러 줄 붙여넣기가 깨진다는 점을 감안하세요.
